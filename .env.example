# Web này chỉ đọc node exams từ Realtime Database, không dùng Firebase Auth API key.
# Firebase Rules cần mở public read riêng node exams:
# "exams": { ".read": true, ".write": "auth != null && ...admin..." }
VITE_FIREBASE_DB_URL=https://ic3-quiz-app-us-default-rtdb.firebaseio.com
VITE_LEVEL2_PREFIX=ic3gs6lv2_
